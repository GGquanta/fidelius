# Fidelius 产品规范

Fidelius 是部署在 Cloudflare Workers 上的小团队敏感信息保险库。它保存服务器账号、SSL 证书、登录凭据等记录。页面入口由 Cloudflare Access 保护；查看敏感内容必须通过 TOTP 开锁。

## 范围

- 单 Worker 全栈：React SPA + Hono API + KV
- Access 邮箱即身份，无第二套账号密码
- 内置用户上限 20 人
- 记录含标题、描述、分类、多个字段
- 自动记录创建时间、修改时间、修改日志
- 按分类提供填写模板
- 概览页聚合当前用户可见记录的计数、分类分布与更新趋势
- 短值复制、长文本下载
- 所有者只读分享；仅所有者可删除与收回分享
- 管理员只管理用户，不自动看到他人记录
- 登录用户可修改自己的显示名；邮箱由 Access 绑定，不可改
- 已绑定用户可在个人资料中更换验证器：须先核对当前验证器或一条未使用的恢复码，新密钥确认后旧密钥与旧恢复码立即作废，并签发新的恢复码

## 角色

| 状态 | 能力 |
| --- | --- |
| 未开通 | 仅看到未授权页；管理员可在团队页看到其 Access 邮箱并开通 |
| `pending_enroll` | 仅可绑定验证器 |
| `member` | CRUD 自己的记录；只读被分享记录；复制/下载；开锁 |
| `admin` | member 全部能力 + 创建/停用用户 + 查看用户列表 |

管理员不能列出、读取、开锁他人未分享的记录。

## 身份

1. Cloudflare Access 校验身份并提供邮箱。
2. 内部用户表按邮箱映射。
3. 用户表为空且邮箱等于 `BOOTSTRAP_ADMIN_EMAIL` 时，该人成为首任管理员并进入验证器绑定。
4. 其他未登记邮箱得到 403，并记入访客列表（不占成员名额，上限 50）。管理员可在团队页点开通，显示名默认为邮箱 `@` 前一段，状态为 `pending_enroll`。
5. 管理员也可直接填写邮箱与显示名添加尚未访问过的成员。新用户首次进入必须完成验证器绑定后才能使用保险库。

## 开锁

- TOTP 为 6 位数字，标准 30 秒窗口。
- 绑定确认后签发 10 条一次性恢复码（`XXXX-XXXX`）。明文只在当次返回，须下载或复制后再进入保险库。
- 开锁与开始更换验证器可用当前验证码，或一条未使用的恢复码；恢复码用过后立即作废。
- 开锁成功后建立 10 分钟会话。
- 列表与详情元数据永不包含敏感值。
- 敏感值仅在开锁会话有效时通过 `reveal` 返回。
- 验证码与恢复码共用失败计数：5 次失败锁定 15 分钟。
- 本地开发可在 `.dev.vars` 设置 `DEV_TOTP_BYPASS`：变量非空且非 production 时，`000000` 可通过绑定、开锁、更换验证器与签发恢复码。生产即使误设该变量也不生效。
- 用户可主动封存。离开 `/records/*` 立即封存。
- 本机设置（`localStorage`，只保存在这台浏览器，不写入服务端）：定时封存（默认开启，15 / 30 / 60 秒，默认 30 秒；从开锁起按墙钟计时，不因鼠标或键盘重置）；离开页面时封存（默认关闭：标签页隐藏或窗口失焦立即封存）。这两项只约束客户端，不改变服务端 10 分钟开锁会话。
- 个人资料可重新生成恢复码（须当前验证码）；旧码全部作废。已绑定但尚未签发过的用户可在此生成。

## 记录

每条记录包含：

- `title`、`description`、`category`
- `fields[]`：键、标签、类型、值（值仅密文存储）
- `ownerId`、`sharedWith[]`
- `createdAt`、`updatedAt`
- 审计日志（最近 100 条；详情按页拉取，每页 10 条）

`title` 与 `description` 为明文元数据，列表与详情会返回；所有者未开锁也可修改。`fields` 的值须开锁后才能更新。已有记录的 `category` 不可更改。

分类：`server`、`database`、`ssl`、`apikey`、`login`、`cloud`、`domain`、`network`、`recovery`、`generic`。模板见 `docs/templates.md`。

## 分享与删除

- 所有者可将记录只读分享给其他 `active` 用户。
- 共享者可查看、复制、下载，不能编辑、删除、再分享。
- 仅所有者可删除记录与收回分享。
- 禁止删除仍拥有记录的用户；须先删除或移交记录。第一期不提供移交，须先删记录。

## 用户故事

1. 首任管理员用匹配邮箱进入，完成验证器绑定，下载并保存恢复码，成为 admin。
2. 管理员创建成员，或开通已访问但未登记的邮箱。成员首次进入完成绑定并保存恢复码。
3. 成员按模板创建服务器密码记录。
4. 成员开锁后复制密码、下载 SSH 私钥。无法使用验证器时，用一条恢复码开锁。
5. 成员把记录只读分享给同事。同事开锁后可看，不能改。
6. 所有者收回分享或删除记录。
7. 管理员停用用户。被停用者无法再访问保险库。
8. 成员打开侧栏用户卡，修改显示名。邮箱不可改。
9. 成员在个人资料中核对当前验证器或一条恢复码后更换验证器。完成后旧密钥与旧恢复码作废，签发新恢复码，开锁会话立即封存。丢失验证器且没有剩余恢复码时，须管理员停用并重建用户。
10. 成员在顶栏打开设置，开关定时封存与离开页面时封存。偏好只写在这台浏览器。

## 非目标（第一期）

- D1、Durable Objects、R2
- 零知识客户端加密
- 公开分享链接、邮件通知
- 超过 20 个用户、组织树
- 证书到期自动巡检
- 用户密码登录、OAuth 应用内登录
- 短信或邮箱自助重置验证器
